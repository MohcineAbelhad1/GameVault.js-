  GNU nano 8.5                  .github/workflows/ci.yml                  Modified
name: Simple node.js CI

on
name: Simple Node.js CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
